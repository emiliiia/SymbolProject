export class Sort {
  constructor(sortField: string, sortDirection: string) {
    this.sortField = sortField;
    this.sortDirection = sortDirection;
  }

  sortField:string;
  sortDirection:string;
}
